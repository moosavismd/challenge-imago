---
# Role execution control (webserver overrides)
install_pre_install: true
install_zabbix: true

# NGINX API Proxy Configuration for Media Service
# Note: Default values are in nginx role defaults/main.yaml
# Override here only if you need different values
