# Deploy Agent Environment Configuration
DEPLOY_TOKEN={{ deploy_agent_token }}
COMPOSE_FILE={{ compose_file }}
COMPOSE_DIR={{ compose_dir }}
PORT={{ deploy_agent_port }}

# API Service Configuration
REGISTRY_IMAGE={{ registry_image }}
